# 🚀 الرفع السريع على Netlify (مجاني وسهل)

بما أنك قمت ببناء التطبيق ولديك مجلد `out`، يمكنك رفعه فوراً دون الحاجة لاستضافة مدفوعة أو لوحة تحكم معقدة.

## الخطوات

1. **تجهيز المجلد**:
   - تأكد أن لديك مجلد `out` في ملفات المشروع (أنت قمت بإنشائه بالفعل).
   - إذا كان مضغوطاً، فك الضغط عنه. نحن نحتاج المجلد الأصفر العادي.

2. **فتح Netlify**:
   - اذهب إلى الرابط: [https://app.netlify.com/drop](https://app.netlify.com/drop)
   - إذا طلب منك التسجيل، سجل مجاناً (Sign up) باستخدام الإيميل.

3. **الرفع (السحب والإفلات)**:
   - ستظهر لك منطقة منقطة مكتوب فيها **"Drag and drop your site output folder here"**.
   - [صورة توضيحية]: 📥 اسحب مجلد `out` بالماوس وأفلته داخل المتصفح.

4. **النشر**:
   - سيبدأ الخط الأخضر بالتحميل.
   - بمجرد الانتهاء، ستتحول الحالة إلى **"Published"** (منشور).
   - سيعطيك رابطاً عشوائياً (مثل: `sultan-sweets-123456.netlify.app`).

5. **تعديل الرابط (اختياري)**:
   - اضغط على **"Site settings"** ⚙️.
   - اضغط على **"Change site name"**.
   - اكتب اسماً سهلاً (مثل `sultan-libya-orders`) واضغط Save.

## ملاحظة هامة جداً

عند استخدام Netlify، قد تحتاج لإضافة ملف بسيط اسمه `_redirects` داخل مجلد `out` لضمان عمل التحديث (Refresh) للصفحات.
(لحسن الحظ، النظام بناه ليعمل كملف HTML واحد في الغالب، لكن هذا احتياط).

إذا واجهت مشكلة "Page Not Found" عند تحديث الصفحة، أنشئ ملفاً نصياً داخل `out` اسمه `_redirects` واكتب بداخله:

```
/*  /index.html  200
```

ثم ارفع المجلد مرة أخرى.
