# دليل نشر تطبيق "طلبات السلطان" على Vercel

هذا الدليل يشرح كيفية رفع التطبيق على منصة Vercel (أفضل منصة لـ Next.js) وربطه بقاعدة بيانات MongoDB.

## المتطلبات

1. حساب على [GitHub](https://github.com).
2. حساب على [Vercel](https://vercel.com) (يمكنك الدخول بحساب GitHub).
3. رابط قاعدة البيانات (MongoDB Connection String) الذي وضعته في `.env.local`.

---

## الخطوة 1: رفع الكود على GitHub

1. اذهب إلى [GitHub](https://github.com/new) وأنشئ **Repository** جديد (سمّه مثلاً `sultan-orders`).
2. لا تضف README أو .gitignore (لدينا بالفعل).
3. بعد الإنشاء، ستظهر لك أوامر ربط المشروع. انسخ الأوامر المشابهة لهذه وشغلها في التيرمينال في مجلد المشروع:

```bash
# استبدل USERNAME باسمك في GitHub
git remote add origin https://github.com/USERNAME/sultan-orders.git
git branch -M main
git push -u origin main
```

---

## الخطوة 2: النشر على Vercel

1. اذهب إلى [لوحة تحكم Vercel](https://vercel.com/dashboard).
2. اضغط زر **"Add New..."** ثم **"Project"**.
3. اختر **"Import"** بجانب مستودع `sultan-orders` الذي رفعته للتو.
4. في صفحة الإعدادات (Configure Project):
   - **Environment Variables**: هذا أهم جزء!
   - اضغط لفتح القائمة، ثم أضف المتغير التالي:
     - **Name**: `MONGODB_URI`
     - **Value**: (الصق رابط قاعدة البيانات الطويل الذي يبدأ بـ `mongodb+srv://...` والذي يحتوي على كلمة المرور الحقيقية).
   - اضغط **Add**.

5. اضغط زر **"Deploy"**.

---

## الخطوة 3: تهانينا! 🎉

سيبدأ Vercel ببناء الموقع. قد يأخذ دقيقة أو دقيقتين.
عند الانتهاء، سيعطيك رابطاً مجانياً (مثلاً `sultan-orders.vercel.app`).

- هذا الرابط يعمل في كل مكان (كمبيوتر، موبايل).
- البيانات محفوظة في MongoDB ومشتركة بين الجميع.
- أي تعديل ترفعه على GitHub سيتم تحديثه في الموقع تلقائياً.
