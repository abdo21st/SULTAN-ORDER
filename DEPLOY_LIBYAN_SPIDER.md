# 🚀 كيفية رفع تطبيق "طلبات السلطان" على استضافة العنكبوت الليبي

بما أن هذا التطبيق تم تصميمه ليعمل بالكامل على المتصفح (Client-Side) ولا يحتاج لخادم خاص، يمكنك استضافته بسهولة على أي خطة استضافة مشتركة (cPanel) مثل استضافة العنكبوت الليبي.

## 1. تجهيز ملفات الموقع

لقد قمنا بضبط الإعدادات لإنتاج نسخة ثابتة (Static HTML).
قم بتشغيل الأمر التالي في التيرمينال لإنشاء نسخة الموقع:

```bash
npm run build
```

بعد انتهاء العملية بنجاح، سيظهر لديك مجلد جديد باسم **`out`** في ملفات المشروع. هذا المجلد يحتوي على موقعك بالكامل.

## 2. ضغط الملفات

1. اذهب إلى المجلد `out`.
2. حدد جميع الملفات والمجلدات بداخله (Ctrl+A).
3. اضغط بزر الفأرة الأيمن واختر **Send to -> Compressed (zipped) folder** (أو اضغطها بصيغة ZIP بأي برنامج).
4. سمِّ الملف `sultan-orders.zip`.

## 3. الرفع على الاستضافة (cPanel)

1. سجل الدخول إلى لوحة تحكم الاستضافة (cPanel).
2. افتح **مدير الملفات (File Manager)**.
3. اذهب إلى مجلد **`public_html`**.
   - إذا كان هناك ملفات قديمة وتريد استبدالها، قم بحذفها (ولكن انتبه لعدم حذف ملفات النظام المهمة إن وجدت).
4. اضغط على زر **Upload (رفع)** في الشريط العلوي.
5. اختر ملف `sultan-orders.zip` الذي قمت بإنشائه.
6. بعد اكتمال الرفع، عد إلى `public_html`.
7. اضغط بزر الفأرة الأيمن على الملف المضغوط واختر **Extract (فك الضغط)**.
8. تأكد من أن الملفات (html, css, js) موجودة مباشرة داخل `public_html` وليست داخل مجلد فرعي. إذا كانت في مجلد فرعي، قم بنقلها للخارج.

## 4. ملف .htaccess (هام جداً)

لقد قمنا بإنشاء ملف `.htaccess` تلقائياً داخل المجلد. هذا الملف وظيفته توجيه أي رابط (مثل `/orders/new`) إلى الصفحة الرئيسية ليعمل التطبيق بشكل صحيح. تأكد من وجوده في `public_html`.
*(ملاحظة: في cPanel، الملفات التي تبدأ بنقطة قد تكون مخفية. اضغط على "Settings" في الزاوية العلوية وأظهر الملفات المخفية "Show Hidden Files" للتأكد).*

## ✅ مبروك

موقعك الآن يعمل. يمكنك الدخول إليه عبر اسم النطاق الخاص بك (Domain).
بما أن البيانات تخزن في المتصفح، كل جهاز سيدخل على الموقع سيكون له بياناته الخاصة المستقلة.
