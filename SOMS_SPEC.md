
**اسم المشروع:** نظام إدارة الطلبات عبر الويب (SOMS - Streamlit Order Management System)
**لغة البرمجة (الأصلية):** Python 3.x
**إطار العمل:** Streamlit
**نظام التخزين:** ملفات JSON (تخزين محلي)

### 1. نظرة عامة على النظام (Project Overview)

هذا التطبيق عبارة عن نظام خفيف لإدارة الطلبات موجه للأعمال الصغيرة. الهدف الأساسي هو تتبع دورة حياة طلبات الزبائن من لحظة التسجيل وحتى التسليم النهائي، مع إدارة الحسابات المالية لكل طلب.

### 2. هيكلية البيانات (Data Structure)

يتم تخزين البيانات داخل قائمة (List) تحتوي على قواميس (Dictionaries) في ملف `orders_db.json`. كل كائن "طلب" يحتوي على الحقول التالية:

* `id` (رقم صحيح): معرف فريد وتسلسلي للطلب.
* `customer` (نص): اسم الزبون.
* `phone` (نص): رقم هاتف الزبون.
* `details` (نص): تفاصيل ومواصفات الطلب.
* `image` (نص/مسار): مسار ملف الصورة المرفقة.
* `date` (نص): تاريخ التسليم المتوقع بصيغة `YYYY-MM-DD`.
* `total` (رقم عشري): القيمة الإجمالية للطلب.
* `paid` (رقم عشري): المبلغ المدفوع (العربون).
* `stage` (نص): المرحلة الحالية للطلب ضمن دورة العمل.

### 3. مراحل سير العمل (Workflow Stages)

يجب أن يمر الطلب عبر مراحل محددة وثابتة (Hardcoded List)، وهي بالترتيب:

1. `"تسجيل الطلب"`
2. `"قيد الانشاء"`
3. `"تم التجهيز"`
4. `"تم النقل"`
5. `"تم التسليم"`

### 4. الوحدات الوظيفية الأساسية (Core Functional Modules)

#### أ. وحدة إدارة البيانات (Data Persistence)

تعتمد على ملف `orders_db.json`.

#### ب. واجهة المستخدم (User Interface)

5 واجهات رئيسية:

1. **إضافة طلب جديد:** نصوص، صور، أرقام (المالية).
2. **عرض الطلبات:** جدول تفاعلي مع عمود "Mablagh" حسابياً (الإجمالي - المدفوع).
3. **تحديث المراحل:** تحديث حالة الطلب فوراً.
4. **البحث:** بحث خطي يطابق النص مع `customer` أو `phone`.
5. **التنبيهات (Deadlines):**
   * 🔴 **تنبيه أحمر:** تجاوز التاريخ.
   * ⚠️ **تنبيه أصفر:** خلال عدد أيام محدد.
   * **استثناء:** تجاهل الطلبات "تم التسليم".
